# Lucide React — Icon Rules

Use Lucide as the default icon library.

## Rules
- Reuse Lucide before adding another icon package.
- Icons must support meaning, not replace text for clinical states.
- Severity and missing-data states require a textual label in addition to color/icon.
- Keep icon sizing consistent with existing components.
- Avoid decorative icon noise in dense clinical views.
